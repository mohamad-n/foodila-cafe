// Persian digits + تومان price formatting for customer-facing UI. Re-exported from the shared
// helper so templates import them from the menu core per the skill's layout.
export { faNum, faToman } from "@/lib/format";
